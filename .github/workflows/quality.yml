name: Quality Gate

on:
  pull_request:
  push:
    branches: [main, master]

permissions:
  contents: read

jobs:
  quality:
    uses: ./.github/workflows/reusable-quality-checks.yml
    with:
      node_versions: '["20", "22"]'
